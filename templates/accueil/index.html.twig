{% extends 'base.html.twig' %}

{% block title %}Accueil Job It{% endblock %}

{% block content %}
{% for c in categos %}
    <div>
    
    
        <!--  -->
        <a href='/job.categorie?nomC={{ c }}'><h2>{{ c }}</h2></a>
        <!--  -->
            <!--  -->
            {% for job in jobs %}
                            {% if job.category.nom == c %}
                                <a href='/jobs?jobId={{ job.id }}' class=''>
                                    <hr>
            <!--  -->
            <section class="p-5 row justify-content-center">
                <img  class="col-3" src="assets/{{job.logo}}" alt="">
                <div class="col-6">
                    <p>{{job.entreprise}}</p>
                    <p>{{job.metier}}</p>
                    <ul>
                        <div class="row justify-content-center">
                        <li>{{job.contrat}}</li>
                        <li>{{job.lieu}}</li>
                        <li>{{job.pays}}</li>
                        </div>
                    </ul>
                </div>
            </section>
            <!--  -->
            {% endif %}
            <!--  -->
            {% endfor %}
            {% endfor %}

        </div>
{% endblock %}
